[package]
name = "ironclaw_llm"
version = "0.1.0"
edition = "2024"
rust-version = "1.92"
description = "Multi-provider LLM integration with retry, failover, circuit breaker, and response caching"
authors = ["NEAR AI <support@near.ai>"]
license = "MIT OR Apache-2.0"
homepage = "https://github.com/nearai/ironclaw"
repository = "https://github.com/nearai/ironclaw"
publish = false

[package.metadata.dist]
dist = false

[features]
default = []
bedrock = ["dep:aws-config", "dep:aws-sdk-bedrockruntime", "dep:aws-smithy-types"]
# Opt-in StubLlm + credentials helpers for downstream test code.
testing = []

[dependencies]
ironclaw_common = { path = "../ironclaw_common", version = "0.4.2" }
ironclaw_safety = { path = "../ironclaw_safety", version = "0.2.2" }

async-trait = "0.1.91"
anyhow = "1.0.104"
base64 = "0.23.0"
bytes = "1.12.1"
chrono = { version = "0.4.45", features = ["serde"] }
dirs = "6"
eventsource-stream = "0.2.3"
futures = "0.3.33"
open = "5.4.0"
rand = "0.10.2"
urlencoding = "2.1.3"
regex = "1.13.1"
reqwest = { version = "0.13.4", default-features = false, features = ["json", "multipart", "rustls-tls-native-roots", "stream"] }
rig-core = { version = "0.40.0", default-features = false, features = ["reqwest-rustls"] }
rust_decimal = { version = "1.42.1", features = ["serde", "serde-with-str", "maths"] }
rust_decimal_macros = "1.40.0"
secrecy = { version = "0.10.3", features = ["serde"] }
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
sha2 = "0.11.0"
thiserror = "2.0.19"
tokio = { version = "1.53.1", features = ["sync", "io-util", "io-std", "time", "macros", "rt", "fs", "process", "net"] }
tracing = "0.1.44"
url = "2.5.8"
uuid = { version = "1.24.0", features = ["v4", "serde"] }

aws-config             = { version = "1.10.1", features = ["behavior-version-latest"], optional = true }
aws-sdk-bedrockruntime = { version = "1.138.0", optional = true }
aws-smithy-types       = { version = "1.6.1", optional = true }

[dev-dependencies]
insta = { version = "1.48.0", features = ["yaml"] }
tempfile = "3.27.0"
tokio = { version = "1.53.1", features = ["full", "test-util"] }
tracing-test = "0.2.6"
